{
  "name": "@mytokens/core",
  "private": true,
  "version": "0.0.0",
  "description": "Lib pura de parsing e agregação de uso de tokens. Sem Electron, sem React.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
